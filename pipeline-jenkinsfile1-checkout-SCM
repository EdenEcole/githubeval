pipeline {
    agent any
    stages {
        stage('Checkout SCM') {
            steps {
                cleanWs()
                git branch: 'main',
                    credentialsId: 'EdenEcole',
                    url: 'https://github.com/EdenEcole/githubeval.git'
            }
        }
    }
}
